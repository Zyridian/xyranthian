import { Spacing } from "../../foundations";

export const $breadcrumbGroupStyles: React.CSSProperties = {
    alignContent: "center",
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    gap: Spacing.spacing2,
    justifyContent: "space-between",
    padding: Spacing.spacing2,
};

export const $breadcrumbListItemStyles: React.CSSProperties = {
    listStyleType: "none",
};
