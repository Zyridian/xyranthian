import Button from "./Button";
import {
    type ButtonProps,
    type ButtonSizes,
    type ButtonVariants,
} from "./types";

export default Button;
export {
    type ButtonProps,
    type ButtonSizes,
    type ButtonVariants,
};
