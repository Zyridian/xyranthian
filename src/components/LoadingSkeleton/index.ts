import LoadingSkeleton from "./LoadingSkeleton";
import {
    type LoadingSkeletonProps,
    type LoadingSkeletonVariants,
} from "./types";

export default LoadingSkeleton;
export {
    type LoadingSkeletonProps,
    type LoadingSkeletonVariants,
};
